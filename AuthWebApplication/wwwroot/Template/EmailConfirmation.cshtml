<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
        }

        .email-container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border: 1px solid #dddddd;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .email-header {
            padding: 20px;
            background-color: #ffffff;
            border-bottom: 1px solid #dddddd;
        }

            .email-header h1 {
                margin: 0;
                font-size: 24px;
                color: #333333;
            }

        .email-body {
            padding: 30px 20px;
        }

            .email-body p {
                margin: 20px 0;
                font-size: 16px;
                color: #555555;
                line-height: 1.5;
            }

        .cta-button {
            display: inline-block;
            margin: 20px auto;
            padding: 12px 24px;
            background-color: #007BFF;
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
        }

            .cta-button:hover {
                background-color: #0056b3;
            }

        .email-footer {
            padding: 20px;
            font-size: 14px;
            color: #777777;
            background-color: #f5f5f5;
            border-top: 1px solid #dddddd;
        }

            .email-footer a {
                color: #007BFF;
                text-decoration: none;
            }

                .email-footer a:hover {
                    text-decoration: underline;
                }
    </style>
</head>
<body>
    <div class="email-container">
        <!-- Header Section -->
        <div class="email-header">
            <h1>Confirm Your Email</h1>
        </div>

        <!-- Body Section -->
        <div class="email-body">
            <p>Dear {{name}},</p>
            <p>Thank you for registering. Please confirm your email address to complete your registration process.</p>
            <a href="{{callbackUrl}}" class="cta-button">Confirm Your Email</a>
            <p>If you did not make this request, please ignore this email.</p>
        </div>

        <!-- Footer Section -->
        <div class="email-footer">
            <p>
                Best regards, <br>
                MRF Technology<br>
                <a href="mailto:pro.maruf.hasnat.eng@gmail.com">Contact Support</a> | &copy; 2024 MRF Technology. All rights reserved.
            </p>
        </div>
    </div>
</body>
</html>
